# Implementation Plans

Generated by the `improve` skill on 2026-08-10, against commit `38d74e8`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Close the confirmed cross-tenant data leaks in visitor followups and user delete/restore | P1 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

None — 001 is standalone.

## Findings considered and rejected

These were raised during the audit, checked against the code, and found not to
be defects. Recorded so they are not re-audited.

- **`churches.controller.ts` `update`/`delete` accept a raw `:id` with no
  ownership check** — rejected. `ChurchContextGuard` is registered globally
  (`app.module.ts:78`) and has a dedicated branch for this route
  (`church-context.guard.ts:37-38`) that resolves `requestedChurchId` from
  `request.params.id` when the path starts with `/churches/`, then requires an
  `active` membership. There is no global route prefix, so that branch is live.
  Non-super-admins cannot reach another church. Super-admins can, by design.

- **`events.service.ts` `setAttendance` RSVP queries lack `churchId`** —
  rejected as a tenancy bug. The method calls `getOwnedEvent(churchId, eventId)`
  on its first line and throws if the event is not owned, so every downstream
  query keyed on `eventId` is transitively scoped. A narrower, real issue
  (unvalidated `attendedMemberIds`) is Step 5 of plan 001.

- **`visitors.service.ts` `convertVisitorToMember` updates `visitorFollowups`
  by `visitorId` only** — rejected. The visitor is ownership-verified at line
  104, before that write.

- **Cent sums cast via `Number()` can overflow 2^53 in `offerings.service.ts`
  and `giving-goals.service.ts`** — rejected. That threshold is ~90 trillion
  cents. Not a real risk for this product.

- **`bg-linear-to-b` in `components/church/setup-church.tsx` is a typo for
  `bg-gradient-to-b`** — rejected. This repo is on Tailwind v4
  (`apps/web/package.json`), where `bg-linear-to-b` is the correct syntax.

## Known findings not yet planned

Surfaced and confirmed, but no plan written yet. Ask for one when you want it.

- No pagination on list endpoints (`members`, `visitors`, `events`, `offerings`
  services) — every one loads a church's full table.
- N+1 query loops in `attendance.service.ts` — batch check-in, open-session
  health, and the daily risk-flag cron.
- Next.js server actions in `apps/web/actions/` accept `churchId` from the
  client without validating it against the session.
- `@Public()` endpoints in `file-upload.controller.ts`: a diagnostics route
  that exposes S3 configuration, and an unauthenticated upload route with no
  endpoint-level size or type cap.
- `apps/web/env.ts` duplicates `env.mjs` with hardcoded localhost fallbacks and
  a default value for the NextAuth signing secret. Likely should be deleted.
- CORS in `apps/api/src/main.ts` sets `origin: '*'` together with
  `credentials: true`. No helmet, no rate limiting.
- Soft-delete filtering is inconsistent — e.g. `zones.service.ts` `getZoneById`
  omits the `deletedAt` predicate that its sibling `getZoneByIdInChurch` has.
- Several foreign keys declare no `onDelete` behaviour
  (`packages/db/tables/visitors.ts`, `familyLifecycle.ts`).
- `offerings.controller.ts` accepts any string as `currency` and permits
  zero-value offerings, while `giving-goals` validates both.
