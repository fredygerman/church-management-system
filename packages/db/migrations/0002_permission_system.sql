-- Phase 1 Permission System Updates
-- Columns church_id and assigned_zone_id already added to users table via db:push
-- This migration serves as a marker for the permission system implementation
-- No new migrations needed - all tables created in 0000 and 0001

SELECT 1;
